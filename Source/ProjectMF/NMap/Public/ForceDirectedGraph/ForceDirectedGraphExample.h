#pragma once

#include "CoreMinimal.h"
#include "ForceDirectedGraph.h"


/*
/ **
 * Example usage of the ForceDirectedGraph class
 * /
class FForceDirectedGraphExample
{
public:
    // Create a simple graph and run the force-directed layout
    static void RunExample();

private:
    // Create sample nodes and edges
    static void CreateSampleGraph(TArray<FForceDirectedGraph::FNode>& OutNodes, TArray<FForceDirectedGraph::FEdge>& OutEdges);

    // Print node positions
    static void PrintNodePositions(const TArray<FForceDirectedGraph::FNode>& Nodes);
};
*/